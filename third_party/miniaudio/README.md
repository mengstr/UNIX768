# miniaudio

Vendored upstream `miniaudio.h` version 0.11.21 from
<https://github.com/mackron/miniaudio>.

Epoch68 uses it only for optional host-side synthesized disk-click output.
The complete upstream license is included in `miniaudio.h`.
