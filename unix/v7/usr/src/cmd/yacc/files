	/* This file has the location of the parser and the program size.
	 * It may also contain definitions to override defaults.  For example,
	 * WORD32 tells yacc that there are at least 32 bits per int.
	 * On some systems the output and temporary names must also change.
	 */

	/* location of the parser text file */
# ifndef PARSER
# define PARSER "/usr/lib/yaccpar"
# endif

	/* basic size of the Yacc implementation */
# define MEDIUM
