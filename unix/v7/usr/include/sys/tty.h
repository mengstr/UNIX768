/*
 * tty structures + tty/ioctl constants -- shared with kernel <tty.h>.
 * (The previous userland copy was stock V7 and had drifted from the kernel:
 * anonymous union member, tun macro, and CINTR = DEL.)
 */
#include "../../sys/h/tty.h"
