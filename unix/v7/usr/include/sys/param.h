/*
 * tunables + machine constants -- shared with kernel <param.h>.
 * (CBSIZE/UMODE/INTPRI etc. are the kernel's 68k values; they are unused by
 * userland, where this copy previously carried stale PDP-11 values.)
 */
#include "../../sys/h/param.h"
