Reserved for a later backend/code generation split.

The extracted ACK tree does not yet map cleanly onto a historical `c2`
directory. This placeholder exists so the top-level layout stays stable while
the real split is determined.
